import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    // 마이페이지 모달
    modalContainer: {
        width: '100%',
        height: '100%',
        paddingTop: 100,
        backgroundColor: ' #fff',
    },
    customBtn: {
        width: '100%',
        height: 50,
        backgroundColor: 'aqua',
    },
});
