spring:
  profiles:
    active: local
    group:
      local: local_server, common, secret
      blue: blue_server, common, secret

---

spring:
  config:
    activate:
      on-profile: local_server
server:
  port: 8080
react:
  server:
    protocol: http
    host: localhost
    post: 5173

---

spring:
  config:
    activate:
      on-profile: blue_server
server:
  port: 8080
react:
  server:
    protocol: https
    host: ??? ?? ?? ??(ip ?? ???)
    post: 5173

---

spring:
  config:
    activate:
      on-profile: common
  servlet:
    multipart:
      max-file-size: 100MB #업로드 가능한 단일 파일 최대 크기
      max-request-size: 1000MB #요청 시 한번에 받을수 있는 파일들의 최대 크기
mybatis:
  mapper-locations:
    - /mappers/*.xml
    - /mappers/user/*.xml